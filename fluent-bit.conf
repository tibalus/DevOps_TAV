[SERVICE]
    Parsers_File /var/log/symfony/parsers.conf

[INPUT]
    Name             tail
    Path             /var/log/symfony/app.log
    Refresh_Interval 5
    Parser           symfony

[OUTPUT]
    Name         loki
    Match        *
    host         ${LOKI_URL}
    port         ${LOKI_PORT}
    drop_single_key on
    line_format json